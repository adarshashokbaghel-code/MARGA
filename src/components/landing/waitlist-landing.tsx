/**
 * Waitlist UI — integrate on top of ShaderShowcase in a follow-up step.
 * Landing page currently uses @/components/ui/hero (ShaderShowcase) as-is.
 */
export function WaitlistLanding() {
  return null;
}
